# QR Code 點名與加分系統　功能規格書

> **版本**：v2（依實作反推，2026-09-11）
> **性質**：這份文件描述系統**目前實際的行為**，而非最初的構想。
> 最初版本的規格書可用 `git show 81f7e28:spec.md` 取回。
> 實作細節與部署步驟見 `README.md`。

---

## 1. 系統目標

讓教師在投影幕上顯示一張 QR Code，全班同學用自己的手機同步完成點名或加分登記，
資料即時寫入 Google 試算表。取代「教師逐一唱名」或「傳紙本簽到表」。

三個核心目標：

1. **自動化出席紀錄** —— 掃碼即簽到，時間戳記由伺服器決定（`YYYY-MM-DD HH:mm:ss`），
   學生改手機時鐘無效。
2. **多類別加分累計** —— 「課堂加分」與「講座加分」各自獨立累加，互不影響。
3. **防止代簽與轉傳** —— 系統無法證明人在教室，但要讓作弊的成本明顯高於出席。

---

## 2. 系統架構

```
學生手機 ──掃 QR──▶ 靜態前端 (Vercel)
                        │  HTTP POST（JSON as text/plain）
                        ▼
              Google Apps Script Web App
                        │  SpreadsheetApp
                        ▼
             Google 試算表（名冊 / 紀錄）
```

| 層 | 技術 | 說明 |
| --- | --- | --- |
| 前端 | 純靜態 HTML/CSS/JS，無建置步驟 | 託管於 Vercel，`cleanUrls` 開啟 |
| 後端 | Google Apps Script Web App | 部署為「執行身分：我／存取權：所有人」 |
| 資料庫 | Google 試算表 | 兩張工作表：`名冊`、`紀錄` |

**無帳號、無登入、無 session cookie。** 學生不需要任何帳號即可簽到。

---

## 3. 角色與流程

### 3.1 教師

1. 開啟教師頁 `/qr`，首次輸入「輪替密語」（存於瀏覽器 localStorage，之後自動帶入）。
2. 選擇模式、填入場次代碼（例如 `0917-1`，每堂課更換）。
3. 按「投影全螢幕」，QR 每 30 秒自動更換，畫面有倒數條。
4. 課後於試算表檢視出席時間與分數；異常情況查「紀錄」工作表。

### 3.2 學生

1. 用手機相機掃描投影幕上的 QR Code。
2. 輸入**學號**（唯一欄位），按送出。
3. 畫面顯示名冊上的姓名（例：「王小明，已記錄簽到時間 …」）以供自我確認。

學號會快取於 localStorage，第二次之後自動帶入，實際操作僅需一次點擊。

---

## 4. 資料模型

### 4.1 「名冊」工作表（9 欄）

A～F 直接來自學務系統匯出的名單，G～I 由系統寫入。

| 欄 | 名稱 | 型態 | 來源 | 說明 |
| --- | --- | --- | --- | --- |
| A | 序號 | 整數 | 名單 | 自動新增者接續最大值 +1 |
| B | 學號 | 文字 | 名單 | **唯一識別鍵**，強制文字格式避免開頭 0 被吃掉 |
| C | 姓名 | 文字 | 名單 | 以名冊為準，學生輸入不會覆寫 |
| D | 年級 | 文字 | 名單 | |
| E | 電子信箱 | 文字 | 名單 | |
| F | 選上否 | 文字 | 名單 | 非名單者自動標記「未在名單」 |
| G | 出席時間 | 日期值 | 系統 | 顯示格式 `yyyy-MM-dd HH:mm:ss`，每次點名覆寫為最新 |
| H | 課堂加分 | 整數 | 系統 | 累加 |
| I | 講座加分 | 整數 | 系統 | 累加 |

### 4.2 「紀錄」工作表（10 欄）

逐筆記錄每次送出，成功與失敗都留。

`送出時間｜模式｜學號｜送出姓名｜名冊姓名｜場次｜分數｜結果｜備註｜裝置`

「裝置」欄為裝置代碼前 8 碼，用於事後辨識「同一支手機送過哪些學號」。

---

## 5. API 規格

### 5.1 端點

單一 Apps Script Web App URL，結尾 `/exec`。

| 方法 | 用途 |
| --- | --- |
| `GET` | 存活性與組態確認 |
| `POST` | 簽到與加分寫入 |

### 5.2 CORS 設計

Apps Script **無法回應 `OPTIONS` preflight**。因此前端以
`Content-Type: text/plain;charset=utf-8` 送出 JSON 字串，使其符合 CORS
「簡單請求」條件，後端自行解析 body。這是本系統跨網域運作的前提，不可改成
`application/json`。

### 5.3 `GET` 回應

```json
{
  "status": "success",
  "message": "QR 點名系統 API 運作中",
  "studentName": "",
  "version": "20260911-idonly-rotate-nonce-normalize-menu",
  "rotateEnabled": true,
  "rosterCount": 33,
  "time": "2026-09-11 13:05:21"
}
```

`version` 用於確認部署的是哪一版程式碼（Apps Script 網頁應用程式跑的是
**版本快照**，不是編輯器裡的即時程式碼）。`rosterCount` 只回數字，不含個資。

### 5.4 `POST` 請求

```json
{
  "mode": "attendance | classBonus | lectureBonus",
  "studentId": "B123456789",
  "session": "0917-1",
  "points": "2",
  "s": "58970051",
  "c": "3a554dee12",
  "dev": "<裝置代碼>",
  "nonce": "<本次送出的識別碼>",
  "token": "<選用固定通行碼>"
}
```

`studentName` 為選填，僅供 API 直接呼叫者使用；學生端一律不送。

### 5.5 `POST` 回應

固定四個欄位：

```json
{ "status": "success|error", "message": "...", "studentName": "...", "suggestion": "" }
```

`suggestion` 僅在查無學號且系統判定為明顯打錯時才有值。

---

## 6. 學生端規格

### 6.1 模式切換

| 網址 | 行為 |
| --- | --- |
| `?mode=attendance` | 寫入 G 欄出席時間（預設值） |
| `?mode=classBonus` | H 欄累加 |
| `?mode=lectureBonus` | I 欄累加 |

### 6.2 學號正規化

前後端共用同一套 `normalizeId()` 規則，**兩邊輸出必須完全一致**：

1. 全形英數符號（U+FF01–U+FF5E）轉半形
2. 全形空白（U+3000）視同空白
3. 若含 `@` 且不在開頭，只取 `@` 之前（學生常直接貼學校信箱）
4. 移除所有空白、`-`、`_`、`.`
5. 轉大寫

輸入框**邊打邊正規化**，並保留游標位置（否則刪字時游標會跳到結尾）。

### 6.3 打錯提示

查無學號時，後端尋找唯一的候選並回傳 `suggestion`，前端顯示一顆
「改用 XXX 送出」按鈕。**不自動套用** —— 簽到成別人比多按一下嚴重得多。

僅處理兩種幾乎確定是轉錄錯誤的情況：

| 類型 | 範例 | 做法 |
| --- | --- | --- |
| 形近字 | `B12345678O` → `B123456780` | 兩邊套同一張對照表（`O/Q→0`、`I/L→1`、`S→5`、`Z→2`、`B→8`、`G→6`）後比對 |
| 漏打開頭字母 | `123456781` → `B123456781` | 比對去掉開頭字母後的數字部分 |

**一般打錯數字不提示**，因為 `B123456783` 的鄰居 `B123456782`、`B123456781`
都是真實同學，猜錯等於幫別人簽到。候選不唯一時亦不提示。

### 6.4 本地儲存

| 鍵 | 內容 | 用途 |
| --- | --- | --- |
| `qr-checkin.student` | 學號 | 下次自動帶入 |
| `qr-checkin.device` | 隨機 UUID | 一機一人判定 |
| `qr-checkin.nonce` | `{sig, nonce, ts}` | 逾時重送的冪等識別 |

所有存取皆包在 `try/catch`，隱私模式取不到時降級運作而非報錯。

---

## 7. 教師端規格（`/qr`）

- 模式、場次、分數、通行碼、輪替密語五個輸入欄，任一變更即重畫 QR。
- 「投影全螢幕」進入大字模式，QR 邊長為視窗短邊的 46%，附倒數條。
- 網址自動推導：支援 `/qr` 與 `/qr.html` 兩種路徑（Vercel `cleanUrls` 會轉址）。
- 輪替密語存於 localStorage，**不進網址、不上傳、學生看不到**。

---

## 8. 防弊機制

### 8.1 輪替式 QR（防截圖轉傳）

教師端每 `ROTATE_PERIOD_SEC`（30）秒重算：

```
slot = floor(現在秒數 / 30)
code = HMAC-SHA256(密語, "mode|session|slot") 取前 10 個 hex 字元
網址 = .../?mode=…&session=…&s=<slot>&c=<code>
```

後端以同一把密語重算比對，並要求 `|伺服器 slot − 送來的 slot| ≤ ROTATE_SLOP_SLOTS`（1）。
容許 ±1 格是留給「掃碼後打字」的時間，一個碼實際有效約 30～90 秒。

**實作要點**：瀏覽器用 Web Crypto，Apps Script 用
`Utilities.computeHmacSha256Signature()`。後者回傳**有號位元組**（32 個裡約
13 個為負），轉 hex 前必須 `b < 0 ? b + 256 : b`，否則兩端永遠對不上。

簽章綁定 `mode` 與 `session`，因此拿點名的碼換加分、或拿上週的碼重用都會被拒。

密語存於**指令碼屬性**而非原始碼（repo 為公開），透過試算表選單的對話框設定。

### 8.2 一機一人

每台裝置首次造訪產生隨機 UUID 存於 localStorage。同一場次內，一台裝置最多可登記
`MAX_STUDENTS_PER_DEVICE`（預設 1）個不同學號，超過即拒絕並指出已幫誰簽過。
`0` 表示不限制。

這是**增加麻煩而非絕對防堵** —— 清除瀏覽器資料或使用無痕視窗即可取得新代碼。
真正的價值在於「紀錄」工作表的裝置欄留下軌跡。

### 8.3 場次去重

`session` 有值時，同一學號在同一場次的加分只計一次（點名不受限，重複掃只是更新時間）。
以 CacheService + PropertiesService 記錄，可由選單清除。

### 8.4 參數竄改防護

- `points` 經 `Math.min(points, MAX_POINTS)` 鉗制，上限 10。
- `mode` 僅接受三個列舉值。
- `studentId` 正規化後須符合 `/^[A-Z0-9]{2,20}$/`。
- 姓名一律以名冊為準，`ROSTER_ONLY` 或未提供姓名時，查無學號直接拒絕，
  不建立空白姓名的資料列。

---

## 9. 併發與可靠性

### 9.1 寫入序列化

全班同時掃碼會產生數十個並行請求。後端以 `LockService.getScriptLock()`
序列化，等待上限 25 秒。若無鎖，兩個請求可能同時讀到「3 分」並都寫「4 分」，
其中一筆消失。

### 9.2 逾時對齊

**前端逾時（45 秒）必須大於後端等鎖上限（25 秒）**，確保學生看到的是後端給的
明確訊息，而非語意不明的「連線逾時」。兩者順序顛倒會導致學生不知是否簽到成功。

### 9.3 冪等

同一筆簽到（`mode|studentId|session`）在 3 分鐘內沿用同一個 `nonce`。
後端於 CacheService 記住成功結果，重送時直接回傳上次結果，不再寫入。
沒有這層保護，逾時後學生重按就會重複加分。

### 9.4 效能

每個請求 3 次試算表 API 呼叫（查學號、寫值、寫紀錄），約 1 秒。
33 人最壞情況循序處理約 30 秒；實際掃碼分散於一兩分鐘內，不會全部同時抵達。
Apps Script 同時執行上限為 30，超過者排隊。

---

## 10. 安全性與個資

| 項目 | 處理方式 |
| --- | --- |
| 學生名單（姓名、信箱） | `name.txt`、`roster.csv` 由 `.gitignore` 與 `.vercelignore` 排除，不進版控、不部署 |
| API 端點 | **本質上公開**。`config.js` 可被任何人讀取，這是架構決定的（學生瀏覽器必須知道打哪裡） |
| 輪替密語 | 存於指令碼屬性與教師瀏覽器 localStorage，不進原始碼 |
| `token` 參數 | 固定通行碼，**僅為速度障礙**，QR 持有者即可見 |
| 原始碼註解 | 不得使用真實學生姓名作為範例 |

**API 公開的實際風險**：陌生人可直接 POST 假資料。緩解手段為
`ROSTER_ONLY = true`（只收名單內學號）、輪替式 QR、以及事後查核紀錄表。

---

## 11. 可設定項

| 常數 | 預設 | 說明 |
| --- | --- | --- |
| `ROTATE_PERIOD_SEC` | 30 | QR 更換週期，調小可壓縮即時轉傳空窗但學生打字較趕 |
| `ROTATE_SLOP_SLOTS` | 1 | 時間格容許誤差 |
| `MAX_STUDENTS_PER_DEVICE` | 1 | 同場次每裝置上限，0 = 不限 |
| `ROSTER_ONLY` | false | true 則完全拒絕名單外學號 |
| `MAX_POINTS` | 10 | `points` 參數上限 |
| `TOKEN` | `''` | 固定通行碼，空字串 = 不驗證 |

試算表選單「點名系統」提供：初始化工作表、設定／停用輪替密語、
清除場次與裝置紀錄、系統狀態。

---

## 12. 部署注意事項

1. Apps Script 網頁應用程式執行的是**版本快照**。改完程式碼務必
   「管理部署作業 → 編輯 → 版本選新版本」，否則線上仍是舊碼。
2. 「執行身分」與「誰可以存取」兩個下拉選單**互相連動**：
   執行身分設為「存取的使用者」時，存取權會隱藏「所有人」選項。
   本系統需要「執行身分 = 我」＋「存取權 = 所有人」。
3. 選「新增部署作業」會產生新網址，須同步更新 `config.js`；
   選「編輯」則網址不變。
4. 前端為純靜態，`git push` 至 `main` 即由 Vercel 自動部署。

---

## 13. 已知限制

| 限制 | 說明 |
| --- | --- |
| 即時轉傳 | 學生視訊轉螢幕給在家同學，對方 30 秒內完成即可通過 |
| 裝置代碼可重置 | 清除瀏覽器資料或無痕視窗即取得新代碼 |
| 無法證明人在教室 | 任何掃碼系統皆無法解決，只能提高成本 |
| API 公開 | 見第 10 節 |
| 借用手機 | 一機一人會誤擋合理情況（手機沒電借同學），需教師手動補登 |

實務建議：QR 僅投影 1～2 分鐘、每堂課更換 `session`、課後檢視紀錄表的裝置欄。
