以下是為你重新輸出的完整系統功能規格書（以**繁體中文**呈現），完全聚焦於系統目標與功能架構：

---

# QR Code 點名與加分系統 功能規格書

### 系統整體架構

* **前端介面 (Front-End Interface)**：託管於雲端平台（Vercel / GitHub Pages）的響應式網頁，提供學生手機進行簽到與加分登記。
* **後端 API 引擎 (Back-End Engine)**：以 Google Apps Script (GAS) 部署之 Serverless Web App API，負責驗證、資料處理與讀寫邏輯。
* **資料庫 (Database / Ledger)**：以 Google 試算表作為核心資料庫，儲存學籍資料、出席時間戳記與累計分數。

---

### 功能規格與系統目標

#### 1. 核心目標 (Core Objectives)

* **目標一：自動化出席紀錄**
* 提供透過掃描課堂專屬 QR Code 進行點名之機制，自動記錄並填入伺服器端精確的時間戳記 (`YYYY-MM-DD HH:mm:ss`)。


* **目標二：多類別加分累計**
* 支援獨立且可累加的點數類別（例如：「課堂加分」與「講座加分」），精確對應至個別學生帳號。


* **目標三：自動化建立與更新帳號**
* 免去前期手動匯入名單的負擔。當學號不存在時自動新增資料列；當學號已存在時，則自動更新最新出席時間或累加分數。



---

#### 2. 用戶與工作流目標 (User & Workflow Goals)

##### A. 學生端互動 (前端網頁)

* **網址參數模式切換**：透過 URL 參數（如 `?mode=attendance`、`?mode=classBonus`、`?mode=lectureBonus`）自動變更網頁簽到模式。
* **輸入驗證與本地記憶**：強制要求輸入「學號」與「姓名」以防匿名提交；自動將學號與姓名快取至裝置本地儲存空間 (localStorage)，方便學生下次快速簽到。
* **即時狀態回饋**：提交表單後，提供即時狀態提示（處理中、簽到成功、錯誤訊息）。

##### B. 教師與課堂工作流

* **集中式 QR Code 投影**：教師僅需在投影幕上展示特定模式之 QR Code，即可讓全班學生同步掃描簽到。
* **去中心化自主簽到**：將點名作業由「教師逐一掃描學生」轉變為「學生自主掃描簽到」，大幅提升課堂點名效率。

---

#### 3. 技術與系統能力 (Technical Capabilities)

* **異步資料傳輸**：採用背景 HTTP `POST` 請求，確保簽到體驗順暢且無需重新整理頁面。
* **跨域存取與公開存取控制**：支援跨域資源共享 (CORS)，允許未驗證之客戶端呼叫 API，並固定回傳標準 JSON 物件 (`status`, `message`, `studentName`)。
* **多請求方法支援**：同時支援 `POST`（資料寫入與更新）與 `GET`（系統存活性確認）請求，避免路由錯誤。

---

#### 4. 資料模型 (Google 試算表欄位結構)

| 欄位欄號 | 欄位名稱 (`JSON 變數`) | 資料型態 / 格式 | 說明 |
| --- | --- | --- | --- |
| **A 欄** | 學號 (`studentId`) | 字串 (String) | 學生唯一識別碼（如：`B11209001`） |
| **B 欄** | 學生姓名 (`studentName`) | 字串 (String) | 該學號對應之學生姓名 |
| **C 欄** | 出席時間 (`attendance`) | 時間戳記 (Timestamp) | 最近一次點名簽到的日期與時間 |
| **D 欄** | 課堂加分 (`classBonus`) | 整數 (Integer) | 累計之課堂參與加分數值 |
| **E 欄** | 講座加分 (`lectureBonus`) | 整數 (Integer) | 累計之講座出席加分數值 |